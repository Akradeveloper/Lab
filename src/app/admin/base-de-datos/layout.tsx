export const metadata = {
  title: "Base de datos - Panel de administración - QA Lab",
  description: "Descargar y restaurar backup de la base de datos",
};

export default function BaseDeDatosLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
